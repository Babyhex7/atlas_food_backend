global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'atlas_food_backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:8080', 'host.docker.internal:8080']
